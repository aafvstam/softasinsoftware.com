@page "/videos"

<h2>PlayList</h2>

<p>Description</p>

<div class="row">
    <div class="col-md-12 text-center">
        <a href="@MoreShowsUrl" class="btn btn-primary" target="_blank">See More Episodes</a>
    </div>
</div>

<!--
@if (Model.ShowUpcomingShows || Model.ShowPreviousShows)
{
    <div class="row">
        @foreach (var show in Model.UpcomingShows)
        {
            <partial name="_YouTubeShow" model="show" />

        }
        @foreach (var show in Model.PreviousShows)
        {
            <partial name="_YouTubeShow" model="show" />
        }
    </div>

    @if (Model.ShowMoreShowsUrl)
    {
    }
}
-->
